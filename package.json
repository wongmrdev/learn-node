{
  "name": "learn-node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only monorepo for teaching Node.js and Fastify concepts via a UI.",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  }
}
